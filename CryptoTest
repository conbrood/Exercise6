public class CryptoTest {

    public static void main(String[] args) throws Exception {
        // The two lines below are there to make sure all arguements are valid amd have been passed.

if (args.length != 3) {
System.out.println("Please give both input and output files");
      
      if else
  
        String inFile = args[0];
    	String outFile = args[1];    			    
    	
    	CryptoEngine cryptoEngine = new CryptoEngine(inFile, outFile);
    	cryptoEngine.encrypt();
    	cryptoEngine.decrypt();
    }
}
